# ADT-1 PDF Auditor Info Extractor 🧾

This script extracts key information from **Form ADT-1 PDF files** such as CIN, company name, auditor details, and appointment type, and saves it as a structured JSON file.

## 📦 Requirements

- Python 3.8+
- Libraries:
  - `PyMuPDF` (install via `pip install pymupdf`)

## 🚀 How to Run

1. Place the signed ADT-1 form PDF (e.g., `Form ADT-1-29092023_signed.pdf`) in the project folder.
2. Run the script:

```bash
python extractor.py
